{
  "name": "sad",
  "version": "1.0.0",
  "description": "nevermind",
  
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel src/index.html",
    "build": "parcel build src/index.html  --public-url /zzz/",
    "deploy": "gh-pages -d dist"
  },
  "author": "Dmytro Lysachenko <dlysachenko98@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.12.0"
  },
  "dependencies": {
    "gh-pages": "^6.1.1"
  }
}
